SIMPLIFY / REVIEW
------------------------------------------------------------------------------
* log is incomplete and hard to read:
	- live() must take module and action
	- show +N -N on separate column
	- add object type in front of debug prefix

* structured errors are complicated:
	- go back to string errors with a prefix for selective catching and
	decision to retry and/or close the file/socket?

* raising and non-raising functions are inconsistent:
	* try_*() functions return plain strings and don't close the file/socket.
	* unprotect_io() converts all string errors to i/o errors.
		* result: raise tbl_err but return nil,str_err, but then protect_io()
		returns nil,tbl_err. also now it's tostring(err) == 'access_denied'
		cuz we don't know if it's str_err or tbl_err.

* error reporting lacking context:
	- unprotect_io() lacks the context that functions with check() have.
	- sometimes even check() lacks context because it's inside try_() code.
* check() from errors.lua is ungreppable because of all local check() flavors.
* check_errno() is not raising like all other check*() functions.

* fs and sock: remove usage of unbuffered read, readn and send (only use buffers).

* fs: atomic save()/saver() rethinking?

* http server smoke test to look for memory leaks and DoS limits

* mdbx test
* mdbx_schema test
* schema test?

* csv.lua uses io.open
* xml.lua uses io.open

LEVERAGE AI
------------------------------------------------------------------------------
* API quality
	* find ways to make API more regular and consistent
	* find ways to decrease API surface but keep it composable
	* find better and more consistent names, args lists, etc.!
	* remove unused code, find functions used only once and inline
	* simplify complicated code
	* find allocations that could be easily prevented, hoisted, etc.
* code quality
	* find obvious bugs
		Where are hidden allocations?
		Where can reentrancy explode?
		What violates my invariants?
	* gen unit tests
	* gen fuzz testing (esp. for http)
* doc quality
	* fix doc inconsistencies
	* write release notes
	* write commit messages


BUGS
------------------------------------------------------------------------------
* WEBB_ACTION dir/file.ext action/arg.json vs action.json/arg
- SCHEMA SYNC make more robust
	* FKs steal indexes (shared indexes)
* CONNPOOL: retry
* ROWSET filter push notifications on session


FEATURES
------------------------------------------------------------------------------
- SCHEMA SYNC insert-or-update rows over existing table
- ROWSET map error messages to fields on check constraint violations.
- glue.lua subst() with varargs like js version
- HTTP CLIENT cookie_default_path() extract fom URI
- TARANTOOL finish extracting metadata
- HTTP ranges on file serving
- HTTP web sockets
- XLS(X) parsing
	- ithub.com/jjensen/lua-xlsx/blob/master/xlsx.lua
	- https://github.com/jjensen/lua-xls
	- https://github.com/bungle/lua-resty-libxl


TASKS (THINK)
------------------------------------------------------------------------------
- wait for all to finish
- kill -> kill all
- terminals bubble up
- attach task to termination of a group of tasks


MULTILANGUAGE
------------------------------------------------------------------------------
TOPICS
	1) identify & cover all sources of text
	2) hot-reloading should update language
	3) centralization of sources
	4) translation UI
	5) language switching UI
SOURCES OF TEXT
	- DONE Lua & JS code: S()  -> source code parser
	- DONE manually adding entries to S_texts() for dynamic strings
	- DONE spa inline html: html()  -> implicit html filter
	- DONE html templates: template.NAME  -> implicit html filter
	- DONE NEW labeled texts in html templates
	- DONE schema field attrs (text, help, etc.) -> ?
	* WIP rowset field_attrs -> ?
	* TODO enum texts
	* TODO rowset & schema attrs (entity_name, entity_names, etc.)
	- DONE UI "lang" slot props -> lang json files


SECURITY
------------------------------------------------------------------------------
- lookup tables are public
	* add role to each restricted table in schema and check that in
	rowset_sql on select -- we could even cut out columns from the result
	set based on that or maybe that's too much


QA
------------------------------------------------------------------------------
- Lua: use a linter
- CI server
	1. protocol for running unit tests: how to run them, how to isolate them,
		and how to interpret the results in a standardized way (need a list
		of test cases and pass/fail status for each).
	2. test machine
	3. use mm for running tests? use github's CI ?
- HTTP stress test
	- continuous random stress testing to find:
	- memory leaks in app code
	- burst stress testing to reach DoS limits and test recovery from DoS
	- discard cookies to test session garbage collection
- Lua: integrate strict.lua
