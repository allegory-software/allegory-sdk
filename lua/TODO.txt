ERROR REPORTING & OWNERSHIP
------------------------------------------------------------------------------
- try_close -> free
	- tls try_close() -> make async

- own: os_thread: synchronized_queue, os_thread_event.
- own: luastate, pthread
- own: zip
- own: mdbx

- remove check()
- review fcall, fpcall, pcall, lua_pcall, xpcall


LOGGING
------------------------------------------------------------------------------
* log is incomplete and hard to read:
	- live() must take module and action
	- show +N -N on separate column
	- add object type in front of debug prefix


WEBB
---------------------------------------------------------------------------
* webb_action: dir/file.ext action/arg.json vs action.json/arg


MDBX
------------------------------------------------------------------------------
* finish DDL
* unit tests
* mdbx_copy incremental + restore (trivial page-block diff, no rolling window.)


MISSING MODULES
------------------------------------------------------------------------------
* AES-GCM, ChaCha20-Poly1305  -- expose from bearssl
* Ed25519 / X25519  -- bind monocypher


LEVERAGE AI
------------------------------------------------------------------------------
* documenting
	* find and document hidden constraints, contracts, rules, invariants.
	* find and document surprising behavior.
	* check/fix API doc inconsistencies, omissions, misleading or wrong phrases.
* debugging
	* check where args are mutated on public APIs
	* check where raising errors after acquiring resources can leak them
	* check where invariants are violated
* refactoring
	* find footguns and surprising behavior
	* find over-abstraction that could be simplified
	* find tri-state variables or variables doing multiple jobs
	* find functions with too many or unrelated responsibilities
	* find functions with complex or confusing signatures
		* find functions of type f(foo, opt) instead of f{foo=, other options}
	* find code paths that are too complex or re-entrant
	* suggest better names for functions, args, fields
	* suggest better API shapes
	* find allocations that could be easily prevented or hoisted
	* remove unused code


LEFTOVERS
------------------------------------------------------------------------------
* csv.lua uses io.open
* xml.lua uses io.open


BUGS
------------------------------------------------------------------------------
* ROWSET filter push notifications on session


FEATURES
------------------------------------------------------------------------------
- SCHEMA SYNC insert-or-update rows over existing table
- ROWSET map error messages to fields on check constraint violations.
- glue.lua subst() with varargs like js version
- HTTP ranges on file serving
- XLS(X) parsing
	- ithub.com/jjensen/lua-xlsx/blob/master/xlsx.lua
	- https://github.com/jjensen/lua-xls
	- https://github.com/bungle/lua-resty-libxl


TASKS (THINK)
------------------------------------------------------------------------------
- wait for all to finish
- kill -> kill all
- terminals bubble up
- attach task to termination of a group of tasks


MULTILANGUAGE
------------------------------------------------------------------------------
TOPICS
	1) identify & cover all sources of text
	2) hot-reloading should update language
	3) centralization of sources
	4) translation UI
	5) language switching UI
SOURCES OF TEXT
	- DONE Lua & JS code: S()  -> source code parser
	- DONE manually adding entries to S_texts() for dynamic strings
	- DONE spa inline html: html()  -> implicit html filter
	- DONE html templates: template.NAME  -> implicit html filter
	- DONE NEW labeled texts in html templates
	- DONE schema field attrs (text, help, etc.) -> ?
	* WIP rowset field_attrs -> ?
	* TODO enum texts
	* TODO rowset & schema attrs (entity_name, entity_names, etc.)
	- DONE UI "lang" slot props -> lang json files


SECURITY
------------------------------------------------------------------------------
- lookup tables are public
	* add role to each restricted table in schema and check that in
	rowset_sql on select -- we could even cut out columns from the result
	set based on that or maybe that's too much

- ASAN dev builds. Build LuaJIT with -fsanitize=address -fno-omit-frame-pointer,
un test suite + a fuzz harness on parser entry points. Won't catch everything
(JIT'd traces aren't instrumented; only the C side and interpreter are),
but catches heap-side overflows reliably. Run before each release if the parsers
change.

- Length cap as a first-class concept. Every external length field should pass
through a single bounded(claimed, max) helper that errors out on overflow.
check str() usage, recv(), send(), pbuffer, etc.
